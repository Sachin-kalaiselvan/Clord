<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=0" />
<body>
</body>

<%
  user_id = 1
  user_hash = OpenSSL::HMAC.hexdigest(
    'sha256',
    @web_widget.hmac_token,
    user_id.to_s
  )

%>
<script>

window.nerixSettings = {
  hideMessageBubble: false,
  // showUnreadMessagesDialog: false,
  // baseDomain: '.loca.lt',
  position: '<%= @widget_position %>',
  locale: 'en',
  useBrowserLanguage: true,
  type: '<%= @widget_type %>',
  // showPopoutButton: true,
  widgetStyle: '<%= @widget_style %>',
  darkMode: '<%= @dark_mode %>',
};

(function(d,t) {
  var BASE_URL = '';
  var g=d.createElement(t),s=d.getElementsByTagName(t)[0];
  g.src= BASE_URL + "/packs/js/sdk.js";
  g.async = true;
  s.parentNode.insertBefore(g,s);
  g.onload=function(){
    window.nerixSDK.run({
      websiteToken: '<%= @web_widget.website_token %>',
      baseUrl: BASE_URL
    })
  }
})(document,"script");

window.addEventListener('nerix:ready', function() {
  console.log('nerix:ready', window.$nerix);
  if (window.location.search.includes('setUser')) {
    window.$nerix.setUser('<%= user_id %>', {
      identifier_hash: '<%= user_hash %>',
      email: 'jane@example.com',
      name: 'Jane Doe',
      phone_number: ''
    });
  }
})

window.addEventListener('nerix:error', function(e) {
  console.log('nerix:error', e.detail)
})


window.addEventListener('nerix:on-message', function(e) {
  console.log('nerix:on-message', e.detail)
})

window.addEventListener('nerix:postback', function(e) {
  console.log('nerix:postback', e.detail)
})

window.addEventListener('nerix:opened', function() {
  console.log('nerix:opened')
})

window.addEventListener('nerix:closed', function() {
  console.log('nerix:closed')
})

window.addEventListener('nerix:on-start-conversation', function(e) {
  console.log('nerix:on-start-conversation', e.detail)
})
</script>
